name: CI/CD Pipeline

on:
  push:
    branches:
      - main  # ou a branch que você deseja monitorar

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Setup SSH
      uses: webfactory/ssh-agent@v0.8.0
      with:
        ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

    - name: Configure Git
      run: |
        git config --global user.email "fernando.rorato@gmail.com"
        git config --global user.name "Fernando Rorato"
        git remote set-url origin https://${{ secrets.GH_TOKEN }}@github.com/fndrorato/oriontax-integrador-cicd.git
  

    - name: Deploy to DigitalOcean Droplet
      run: |
        ssh -o StrictHostKeyChecking=no root@174.138.73.68 '
        cd /var/www/orion-ci-cd &&
        git pull origin main &&
        source venv/bin/activate &&  # Se você estiver usando um virtualenv
        pip install -r requirements.txt &&
        systemctl restart gunicorn  # Substitua pelo seu serviço real
        '
